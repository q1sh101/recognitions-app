<!DOCTYPE html>
<html>

<head>
    <title><%= content_for(:title) || "Recognize Fe Test App" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <link rel="icon" href="/favicon.ico?v=5" type="image/x-icon">

    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <!-- Vite: assets (styles + custom JS) -->
    <%= vite_client_tag %>
    <%= vite_stylesheet_tag 'styles.scss' %>
    <%= vite_javascript_tag 'application' %>

    <!-- Rails: Turbo / Stimulus via importmap -->
    <%= javascript_importmap_tags %>

</head>

<body>
    <nav class="navbar navbar-expand-sm navbar-light bg-light fixed-top border-bottom px-3">
        <a class="navbar-brand fw-semibold" href="/">Recognitions</a>
        <div class="ms-auto d-flex gap-2 align-items-center">
            <% if logged_in? %>
            <span class="small text-muted">Hi, <%= current_user.name %></span>
            <%= button_to 'Logout', logout_path, method: :delete, class: 'btn btn-outline-secondary btn-sm' %>
            <% else %>
            <% unless current_page?(login_path) %>
            <%= link_to 'Login', login_path, class: 'btn btn-primary btn-sm' %>
            <% end %>
            <% end %>
        </div>
    </nav>

    <!-- Flash Messages - Modern Toast Style -->
    <div class="position-fixed top-0 end-0 p-3 mt-5 z-3">
        <% flash.each do |k,v| %>
        <% next if k.to_s.end_with?('_error') %>
        <div class="toast show align-items-center text-white bg-<%= k == 'alert' ? 'danger' : 'success' %> border-0 mb-2" role="alert" aria-live="assertive" aria-atomic="true" data-bs-autohide="true" data-bs-delay="5000">
            <div class="d-flex">
                <div class="toast-body">
                    <%= v %>
                </div>
                <button type="button" class="btn-close btn-close-white me-2 m-auto" data-bs-dismiss="toast" aria-label="Close"></button>
            </div>
        </div>
        <% end %>
    </div>

    <main class="container mt-4">
        <%= yield %>
    </main>
</body>

</html>